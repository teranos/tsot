[package]
name = "laye-protocol"
version = "0.0.0"
edition = "2024"
description = "Pure-data wire types."
license = "MIT"

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "=1.0.143"
