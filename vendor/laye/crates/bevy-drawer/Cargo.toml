[package]
name = "bevy-drawer"
version = "0.0.0"
edition = "2024"
description = "In-canvas diagnostic view — reads ErrorLog into a formatted line buffer."
license = "MIT"

[dependencies]
bevy-observability = { path = "../bevy-observability" }
bevy_app = { version = "=0.19.0", default-features = false }
bevy_ecs = { version = "=0.19.0", default-features = false }
