//! Empty lib — the integration test lives in tests/. This crate
//! exists only so cargo has somewhere to put the test that does not
//! drag the rave wasm32 lib (and therefore Bevy + winit) into the
//! Linux CI build.
