# Use Bevy 0.18 (wgpu + ECS) as the render + frame loop. Lives at [`universe/`](../../../universe/). Forced by 3D voxels — hand-rolled WebGL2 + eframe was never going to fit the voxel framing in `roam/README.md`. **Drop egui entirely at v0.5.1**: rewrite `roam/src/ui/mod.rs` straight into Bevy's in-core `bevy_ui`. 0.18 chosen over 0.19 because `bevy_voxel_world` tracks 0.18. Migrate to 0.19 when `bevy_voxel_world` supports it.

https://github.com/bevyengine/bevy
