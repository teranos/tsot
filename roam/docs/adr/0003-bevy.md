# Switch roam's render + frame loop to Bevy `=0.18` (wgpu + ECS). Forced by 3D voxels — hand-rolled WebGL2 + eframe was never going to fit the voxel framing in `roam/README.md`. **Drop egui entirely at v0.5.1**: rewrite `roam/src/ui/mod.rs` straight into Bevy's in-core `bevy_ui` (Flexbox-based, ECS-native, no adapter layer). 0.18 chosen over 0.19 because `bevy_voxel_world`'s main currently tracks 0.18; v0.5.5 voxel render lands without a wait. Spike (v0.5.0, this session) compiled cleanly on roam's wasm stack after loosening `sacred-error` + `tsot-card` serde pins from `=1.0.219` to `1.0.219` (Bevy's transitive `wgpu-types` / `hashbrown` want ≥1.0.220).

https://github.com/bevyengine/bevy
