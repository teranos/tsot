# Use Bevy (wgpu + ECS) as the render + frame loop. Lives at [`universe/`](../../../universe/). Forced by 3D voxels — hand-rolled WebGL2 + eframe was never going to fit the voxel framing in `roam/README.md`. **Drop egui entirely at v0.5.1**: rewrite `roam/src/ui/mod.rs` straight into Bevy's in-core `bevy_ui`.

Cell stage is 2D — Spore precedent.

https://github.com/bevyengine/bevy
