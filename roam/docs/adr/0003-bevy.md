# Use Bevy (wgpu + ECS) as the render + frame loop. Lives at [`universe/`](../../../universe/) — fresh code, not a port of roam. roam stays at v0.4.x; new work happens in universe from the cells stage up. Forced by 3D voxels — hand-rolled WebGL2 + eframe was never going to fit the voxel framing in `roam/README.md`.

Cell stage is 2D — Spore precedent.

https://github.com/bevyengine/bevy
