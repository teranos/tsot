[package]
name = "tsot"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "tsot"
path = "src/main.rs"

[dependencies]
mlua = { version = "0.10", features = ["lua54", "vendored"] }
rand = "0.8"
