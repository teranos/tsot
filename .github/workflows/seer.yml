name: seer

# Two jobs, one workflow. Both prove the same code compiles for both
# targets and runs under both runtimes with full observability output.
#
#   seer-native — runs the game as a native binary. Rust backtrace
#                 support gives Rust source paths on every >= 64 KB
#                 allocation. Fastest dev cycle: build + run in seconds.
#
#   seer-host   — builds the game as wasm32-unknown-unknown, then loads
#                 it into the wasmtime host binary (crates/seer-host).
#                 Every wasm→host boundary crossing is a recorded event.
#                 This is what the "wasmtime as primary diagnostic
#                 environment" requirement means concretely: total
#                 host-side introspection of a real wasm running the
#                 same source that will later deploy to the browser.
#
# No browser, no simulator, no headless Chrome. Both jobs upload their
# stdout as artifacts for inspection.

on:
  push:
    branches: ['**']
    paths:
      - 'seer/**'
      - 'crates/seer-host/**'
      - '.github/workflows/seer.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  seer-native:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: seer

      - name: Install Mesa software Vulkan (lavapipe)
        run: |
          sudo apt-get update
          sudo apt-get install -y mesa-vulkan-drivers libvulkan1

      - name: Build seer-native
        working-directory: seer
        env:
          SEER_BUILD_COMMIT: ${{ github.sha }}
          SEER_BUILD_TIME: ${{ github.event.head_commit.timestamp }}
        run: cargo build --release --bin seer-native

      - name: Run seer-native
        working-directory: seer
        env:
          RUST_BACKTRACE: full
          SEER_FRAMES: '180'
          SEER_FRAME_PATH: out/frame.png
        run: |
          mkdir -p out
          cargo run --release --bin seer-native 2>&1 | tee out/seer-native.log
          ls -la out/

      - name: Assume deploy role
        if: success() && vars.SEER_DISTRIBUTION_ID != ''
        uses: aws-actions/configure-aws-credentials@v5
        with:
          role-to-assume: arn:aws:iam::548351057127:role/seer-github-deploy
          aws-region: eu-central-1

      - name: Upload rendered frame to S3
        if: success() && vars.SEER_DISTRIBUTION_ID != ''
        run: |
          if [ -f seer/out/frame.png ]; then
            for prefix in "perf/${{ github.sha }}" "perf/latest"; do
              aws s3 cp seer/out/frame.png "s3://seer-sbvh-static/$prefix/frame.png" \
                --cache-control "public, max-age=300" \
                --content-type "image/png"
            done
            aws cloudfront create-invalidation \
              --distribution-id "${{ vars.SEER_DISTRIBUTION_ID }}" \
              --paths '/perf/latest/frame.png' \
              --query 'Invalidation.Id' --output text
          fi

      - name: Upload native log
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: seer-native-${{ github.sha }}
          path: seer/out/
          retention-days: 30

  seer-host:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - name: Install wasm32 target
        run: rustup target add wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2

      - name: Build seer.wasm
        working-directory: seer
        env:
          SEER_BUILD_COMMIT: ${{ github.sha }}
          SEER_BUILD_TIME: ${{ github.event.head_commit.timestamp }}
        run: cargo build --release --lib --target wasm32-unknown-unknown

      - name: Build seer-host
        working-directory: crates/seer-host
        run: cargo build --release

      - name: Fetch prior history from S3
        if: vars.SEER_DISTRIBUTION_ID != ''
        continue-on-error: true
        run: |
          mkdir -p out
          # Assume role BEFORE fetching (config is separate step); this
          # runs unauthenticated against a public GET which CloudFront
          # serves. Falls back to empty history on 404 or perm issue.
          curl -sSf -o out/history-in.json https://seer.sbvh.nl/perf/history.json || echo '[]' > out/history-in.json
          echo "prior history entries: $(python3 -c 'import json,sys;print(len(json.load(open("out/history-in.json"))))' 2>/dev/null || echo 0)"

      - name: Run seer-host on seer.wasm
        env:
          SEER_FRAMES: '180'
          SEER_REPORT_PATH: out/report.html
          SEER_HISTORY_IN_PATH: out/history-in.json
          SEER_HISTORY_OUT_PATH: out/history.json
          SEER_SUMMARY_OUT_PATH: out/summary.json
          SEER_VERDICT_OUT_PATH: out/verdict.json
        run: |
          mkdir -p out
          [ -f out/history-in.json ] || echo '[]' > out/history-in.json
          ./crates/seer-host/target/release/seer-host \
            seer/target/wasm32-unknown-unknown/release/seer.wasm \
            2>&1 | tee out/seer-host.log
          ls -la out/

      - name: Upload host log
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: seer-host-${{ github.sha }}
          path: out/
          retention-days: 30

      - name: Assume deploy role
        if: success() && vars.SEER_DISTRIBUTION_ID != ''
        uses: aws-actions/configure-aws-credentials@v5
        with:
          role-to-assume: arn:aws:iam::548351057127:role/seer-github-deploy
          aws-region: eu-central-1

      - name: Upload perf report to S3
        if: success() && vars.SEER_DISTRIBUTION_ID != ''
        run: |
          # Two locations: /perf/<sha>/ for permalinks, /perf/latest/
          # for the always-current head.
          for prefix in "perf/${{ github.sha }}" "perf/latest"; do
            aws s3 cp out/report.html "s3://seer-sbvh-static/$prefix/report.html" \
              --cache-control "public, max-age=300" \
              --content-type "text/html; charset=utf-8"
            aws s3 cp out/seer-host.log "s3://seer-sbvh-static/$prefix/seer-host.log" \
              --cache-control "public, max-age=300" \
              --content-type "text/plain; charset=utf-8"
            if [ -f out/summary.json ]; then
              aws s3 cp out/summary.json "s3://seer-sbvh-static/$prefix/summary.json" \
                --cache-control "public, max-age=300" \
                --content-type "application/json"
            fi
          done
          # Global rolling history — no per-sha prefix.
          if [ -f out/history.json ]; then
            aws s3 cp out/history.json "s3://seer-sbvh-static/perf/history.json" \
              --cache-control "public, max-age=60" \
              --content-type "application/json"
          fi
          aws cloudfront create-invalidation \
            --distribution-id "${{ vars.SEER_DISTRIBUTION_ID }}" \
            --paths '/perf/latest/*' '/perf/history.json' \
            --query 'Invalidation.Id' \
            --output text

      - name: Enforce verdict
        if: always()
        run: |
          # Check verdict AFTER upload — a failing report is still a
          # useful report and needs to be visible. This step is what
          # actually turns the job red on threshold violation.
          if [ -f out/verdict.json ]; then
            PASSED=$(python3 -c 'import json,sys;print(json.load(open("out/verdict.json"))["passed"])')
            echo "verdict.passed = $PASSED"
            if [ "$PASSED" != "True" ]; then
              echo "::error::seer threshold violated — see report + verdict.json artifact"
              cat out/verdict.json
              exit 1
            fi
          fi
